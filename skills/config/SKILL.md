# Config Sub-Skill

Handles configuration management for the paper-reader skill.

## Commands

- `config show` — Display current configuration
- `config set <key> <value>` — Set a configuration value
- `config reset` — Reset configuration to defaults
